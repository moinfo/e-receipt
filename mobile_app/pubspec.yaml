name: e_receipt_app
description: E-Receipt Management System Mobile App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.2

  # State Management
  provider: ^6.0.5

  # HTTP & API
  http: ^1.1.0

  # Local Storage
  shared_preferences: ^2.2.2

  # Image Picker
  image_picker: ^1.0.4
  file_picker: ^8.0.0

  # Permissions
  permission_handler: ^11.0.1

  # Date/Time
  intl: ^0.18.1

  # Loading & Dialogs
  flutter_spinkit: ^5.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # Add assets here if needed
  # assets:
  #   - images/
